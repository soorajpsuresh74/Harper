import 'package:flutter/cupertino.dart';

class Maintainability extends StatelessWidget {
  const Maintainability({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text('A'),
        Text('B'),
        Text('C'),
        Text('D'),
        Text('E'),
      ],
    );
  }
}
